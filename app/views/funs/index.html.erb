<!-- https://developers.google.com/maps/documentation/javascript/tutorial <-- code without notes -->

<!-- The div with the id of map is so that the map has somewhere to go - like react! -->
<!--
The map can be pulled and moved just like a normal map!!! When you zoom out too far it will tile, though -->


<html>
  <head>
    <title>Simple Map</title>
    <meta name="viewport" content="initial-scale=1.0">
    <meta charset="utf-8">
    <style>
      /* Always set the map height explicitly to define the size of the div
       * element that contains the map. */
      #map {
        height: 100%;
      }
/* Optional: Makes the sample page fill the window. */

/* Percentages inherit from their parent component. If none of those anscestors have a size, they are assumed to be 0x0 pixels... which could be an issue.  This way our highest tag has a size applied, so evrything else can inherit from it */

/*
Where to go to draw on map:
https://developers.google.com/maps/documentation/javascript/datalayer

*/


/* Can write CSS inside of style tages! */
html, body {
  height: 100%;
  margin: 0;
  padding: 0;
}
</style>
</head>
<body>
<div id="map"></div>
<script>
  //can write javascript inside of a script tag
  var map;
  function initMap() {
    map = new google.maps.Map(document.getElementById('map'), {

      //center and zoom are both required options
      center: {lat: 42.3601, lng: -71.0589},

      //zoom: 0 means a WHOLE GLOBE - must be an integer
      zoom: 4

// 1: World
// 5: Landmass/continent
// 10: City
// 15: Streets
// 20: Buildings
    });
  }
</script>

<!--
Original Code from the site:

<script src="https://maps.googleapis.com/maps/api/js?key=<%= @key %>&callback=initMap"
async defer>
//breakdown of this image tag: the url is the location of a javascript file that loads all of the symbols and definitions we'll need = aka, this tag is REQUIRED

//async - lets browser render other things instead of waiting

//callback - when the api is ready, it will call the func specified using a callback
// in this example, the CALLBACK is INITMAP (aka the function we defined above)

//link MUST be https:// NOT http://
</script> -->

<script src="https://maps.googleapis.com/maps/api/js?key=<%= @key %>&libraries=visualization&callback=initMap" async defer></script>
</script>
</body>
</html>
